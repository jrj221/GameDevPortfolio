{
  "name": "gamedevportfolio-backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "sam": "sam build && sam deploy"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.996.4",
    "@aws-sdk/util-dynamodb": "^3.996.4"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.149",
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
